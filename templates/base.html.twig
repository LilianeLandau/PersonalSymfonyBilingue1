<!DOCTYPE html>
<!--cette ligne de code permet de définir dynamiquement 
la langue du document HTML en fonction de la locale de la requête -->
<html lang="{{ app.request.locale }}">
	<head>
		<meta charset="UTF-8">
		<meta
		name="viewport" content="width=device-width, initial-scale=1.0">

		<!-- Favicon -->
		<link
		rel="icon" href="{{ asset('favicon.png') }}" type="image/png">

		<!-- dans les autres templates, ce bloc sera remplacé par un titre spécifique à la page-->
		<title>
			{% block title %}Mon Site Bilingue1
			{% endblock %}
		</title>

		<!-- Bootstrap -->
		<link
		rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">

		<!-- Styles personnalisés -->
		<style>
			/* Conteneur pour les drapeaux */
			.language-switcher {
				display: flex;
				justify-content: center;
				gap: 15px;
				margin: 20px 0;
			}

			/* Style des drapeaux */
			.flag-icon {
				width: 40px; /* Taille du drapeau */
				height: 30px;
				border-radius: 5px; /* Coins arrondis */
				box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2); /* Ombre légère */
				transition: transform 0.2s ease-in-out;
			}

			/* Effet de zoom au survol */
			.flag-icon:hover {
				transform: scale(1.1);
			}

			/* Style des liens */
			.language-switcher a {
				display: flex;
				flex-direction: column;
				align-items: center;
				text-decoration: none;
				color: #333;
				font-weight: bold;
				font-size: 14px;
			}

			.language-switcher a:hover {
				color: #007bff; /* Bleu Bootstrap */
			}
		</style>
	</head>

	<body>
		<div
			class="container text-center mt-4">
			<!-- Sélecteur de langue -->
			<div class="language-switcher">
				<a href="{{ path('home', {'_locale': 'fr'}) }}">
					<img src="{{ asset('images/fr.png') }}" alt="Français" class="flag-icon">
					<span>Français</span>
				</a>
				<a href="{{ path('home', {'_locale': 'en'}) }}">
					<img src="{{ asset('images/en.png') }}" alt="English" class="flag-icon">
					<span>English</span>
				</a>
			</div>

			<!-- Contenu de la page / sera remplacé par le contenu spécique de chaque page -->
			{% block body %}{% endblock %}
		</div>

		<!-- Bootstrap JS -->
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
	</body>
</html>
